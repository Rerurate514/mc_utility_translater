import 'package:flutter/material.dart';

class ExplainEditText extends StatelessWidget {
  const ExplainEditText({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("右のJSONエディタでJSONを編集する。");
  }
}
